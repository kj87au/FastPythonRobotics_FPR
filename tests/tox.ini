[tox]
skipsdist = true
envlist = py310

[testenv]
whitelist_externals = poetry
commands =
    poetry install -v
    poetry run pytest

[pytest]
minversion = 6.0
addopts = -ra -q
pythonpath = FPR
testpaths =
    tests/unit
    tests/integration
